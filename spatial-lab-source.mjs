/* UTF-8 · WebGL 实体硬件、同场景 CSS3D 菜单和独立数据层。 */
import * as T from 'three';
import {OrbitControls} from 'three/addons/controls/OrbitControls.js';
import {CSS3DRenderer,CSS3DObject} from 'three/addons/renderers/CSS3DRenderer.js';
import {RoomEnvironment} from 'three/addons/environments/RoomEnvironment.js';
import {buildHardware} from './spatial-hardware.mjs';
import {RUBIN_SPEC,RUBIN_SOURCES} from './rubin-hardware.mjs';

function boot(){
const N=window.NETWORK_MODEL,L=window.LPX_MODEL;
const host=document.createElement('main');host.id='spatial-lab';host.setAttribute('aria-label','Groq LPX 三维空间');document.body.append(host);
let renderer;try{renderer=new T.WebGLRenderer({antialias:true,alpha:false,powerPreference:'high-performance'});}catch(error){host.innerHTML='<div class="sl-error">当前浏览器无法创建 WebGL 场景。请使用支持 WebGL 的浏览器打开此 HTML。<button id="sl-fallback">进入原版解析</button></div>';document.body.dataset.workspace='spatial';document.getElementById('sl-fallback').onclick=()=>window.GROQ3_SPACE_APP.activate();return;}
renderer.domElement.className='sl-render';renderer.domElement.setAttribute('aria-label','拖动旋转硬件，滚轮缩放，双击槽位或芯片深入');renderer.domElement.tabIndex=0;host.append(renderer.domElement);
renderer.setPixelRatio(Math.min(devicePixelRatio,1.7));renderer.outputColorSpace=T.SRGBColorSpace;renderer.toneMapping=T.ACESFilmicToneMapping;renderer.toneMappingExposure=1.05;renderer.shadowMap.enabled=true;renderer.shadowMap.type=T.PCFSoftShadowMap;
const css=new CSS3DRenderer();css.domElement.className='sl-css';host.append(css.domElement);
const scene=new T.Scene();scene.background=new T.Color('#ccd0d3');scene.fog=new T.Fog('#ccd0d3',65,115);
const camera=new T.PerspectiveCamera(38,1,.05,180);scene.add(camera);
const env=new RoomEnvironment(),pmrem=new T.PMREMGenerator(renderer);scene.environment=pmrem.fromScene(env,.04).texture;env.dispose();pmrem.dispose();
scene.add(new T.HemisphereLight(0xf5fffa,0x8e9e9a,1.4));
const key=new T.DirectionalLight(0xfff8df,3.5);key.position.set(-7,18,12);key.castShadow=true;key.shadow.mapSize.set(2048,2048);Object.assign(key.shadow.camera,{left:-16,right:16,top:16,bottom:-16,near:.5,far:60});key.shadow.bias=-.0005;scene.add(key);
const rim=new T.DirectionalLight(0xc6edff,2.5);rim.position.set(10,8,-12);scene.add(rim);
const floor=new T.Mesh(new T.PlaneGeometry(240,240),new T.MeshStandardMaterial({color:0xd9dfd9,roughness:.8,metalness:.15}));floor.rotation.x=-Math.PI/2;floor.position.y=-6.25;floor.receiveShadow=true;scene.add(floor);const grid=new T.GridHelper(80,80,0x959a9f,0xb4b9be);grid.position.y=-6.22;grid.material.transparent=true;grid.material.opacity=.32;scene.add(grid);
const controls=new OrbitControls(camera,renderer.domElement);controls.enableDamping=true;controls.dampingFactor=.085;controls.minDistance=10;controls.maxDistance=75;controls.maxPolarAngle=Math.PI*.82;controls.enablePan=true;
const state={model:'q27',mode:'decode',routeMode:'request',layer:1,weightBytes:1,tokenIndex:0,outputIndex:0,expert:67,rack:0,slot:0,chip:0};
let steps=workflow(),index=0,level='external',follow=true,running=false,elapsed=0,last=0,speed=1,explosion=0,explosionTarget=0,hardware=null,picks=[],manualPart='',complete=false,layoutInfo={},diagramOpen=false;
const duration=5.2,panels={},dataLayer=new T.Group();dataLayer.name='Token 数据层 · 独立于硬件';scene.add(dataLayer);let trajectories=[],signal=null,activeLabel=null;
function workflow(){const rows=N.trace(state);return state.routeMode==='decode'?rows.filter(x=>!['prompt','tokenize','prefill'].includes(x.id)):rows;}
function currentShape(){return N.shape(state.routeMode==='request'&&['prompt','tokenize','prefill'].includes(steps[index].id)?{...state,mode:'prefill'}:state);}
const levelNames={external:'外部',gpu:'GPU',rack:'机架',tray:'托盘',fabric:'互联',chip:'芯片'};
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fnum=n=>n===null||n===undefined?'未公开':n>=1e9?(n/1e9).toFixed(2)+' G':n>=1e6?(n/1e6).toFixed(2)+' M':n>=1e3?(n/1e3).toFixed(2)+' K':n.toFixed(0);
const bytes=n=>n===null||n===undefined?'—':n>=2**20?(n/2**20).toFixed(2)+' MiB':n>=1024?(n/1024).toFixed(2)+' KiB':n+' B';
function panel(id,cls,html){const el=document.createElement('section');el.className='sl-panel '+cls;el.dataset.spatialPanel=id;el.innerHTML=html;css.domElement.append(el);const obj=new CSS3DObject(el);obj.name='悬浮菜单 · '+id;obj.userData.menu=true;scene.add(obj);panels[id]={el,obj};return el;}
panel('title','sl-title',`<h1><span class="sl-brandmark">g.</span>Groq LPX</h1><div class="sl-workspaces"><span class="active">布局</span><button data-action="network">模型网络</button><button data-action="sources">资料</button></div><small id="sl-request-status">请求 A · 单次输入</small>`);
panel('left','sl-left',`<div><div class="sl-eyebrow">EXECUTION OUTLINER</div><h2>执行树</h2></div><div class="sl-model"><select id="sl-model" aria-label="模型"><option value="q27">Qwen3.8-27B · Dense</option><option value="flash">Qwen3.8-Flash-Next · MoE</option></select><div class="sl-row"><button data-mode="request">完整请求</button><button data-mode="prefill">Prefill</button><button data-mode="decode">Decode</button></div><div class="sl-layer"><label for="sl-layer">展开层</label><select id="sl-layer"></select></div><div id="sl-model-hint" class="sl-muted"></div></div><nav id="sl-flow" class="sl-scroll sl-flow" aria-label="完整执行流程"></nav><div class="sl-streams"><div class="sl-instruction"><b>指令</b><span id="sl-instruction"></span></div><div class="sl-data"><b>数据</b><span id="sl-data"></span></div><div class="sl-muted">算法操作回放；实际编译指令与逐周期轨迹未公开。</div></div>`);
panel('right','sl-right',`<div><div class="sl-eyebrow">PROPERTIES</div><h2>属性 · 运算与性能</h2></div><div><div class="sl-muted" id="sl-time-label">资源下界</div><div class="sl-time" id="sl-time"></div></div><div class="sl-metrics"><div><span id="sl-byte-label">数据规模</span><strong id="sl-bytes"></strong></div><div><span>矩阵算量</span><strong id="sl-flops"></strong></div><div><span>计算位置</span><strong id="sl-hw"></strong></div><div><span>有效上下文</span><strong id="sl-context"></strong></div></div><div class="sl-scroll" style="flex:1"><h3 id="sl-step-title"></h3><div class="sl-equation" id="sl-formula"></div><p id="sl-detail" class="sl-spec"></p><p id="sl-rule" class="sl-muted" style="margin-top:10px"></p><div class="sl-part"><div class="sl-eyebrow">HARDWARE / PLACEMENT</div><div id="sl-part"></div><div id="sl-placement" class="sl-muted"></div><div class="sl-row" id="sl-expert-row" style="margin-top:8px"><label for="sl-expert">跟踪专家</label><select id="sl-expert">${L.exampleExperts.map(e=>`<option value="${e}">${e}</option>`).join('')}</select></div><div class="sl-row" style="margin-top:8px"><label for="sl-precision">权重</label><select id="sl-precision"><option value="1">FP8 · 1 B</option><option value="2">BF16 · 2 B</option></select></div></div><details class="sl-sources" id="sl-sources"><summary>公开结构与边界</summary><div id="sl-source-links"></div><p class="sl-muted">外形按公开图重建，非制造 CAD。板内连线为功能连接；微小网格仅为表面纹理，不代表执行单元数量。Rubin 封装含双计算裸片与八组 HBM4，按 NVIDIA 官方图重建；SM 的实际任务分配和微小电路纹理不视为公开 floorplan。数值为形状与资源下界，动画秒数不等于硬件时间。</p></details></div>`);
panel('toolbar','sl-toolbar',`${Object.entries(levelNames).map(([key,name])=>`<button data-level="${key}">${name}</button>`).join('')}<span class="sl-divider"></span><button data-action="follow" title="画面跟随当前算子">跟随</button><button data-action="fit" title="恢复当前硬件视角">复位</button><button data-action="explode" title="抽出托盘或拆开服务器">展开</button><span class="sl-divider"></span><button data-action="front">Z 正视</button><button data-action="top">Y 顶视</button>`);
panel('caption','sl-caption','');
panel('bottom','sl-bottom',`<div class="sl-prompt"><span>谁是世界上最厉害的大模型？</span><button data-action="template">正文 6 / 输入 18</button></div><div id="sl-tokens" class="sl-tokenlist"></div><div class="sl-timeline" id="sl-timeline" aria-label="执行时间轴"></div><div class="sl-progress"><span id="sl-progress"></span></div><div class="sl-playback"><div class="sl-row"><button data-action="reset" title="重置请求，从外部重新输入">↺</button><button data-action="prev" title="上一个操作">←</button><button data-action="play" class="sl-primary">播放</button><button data-action="next" title="下一个操作">→</button><span id="sl-counter" class="sl-muted"></span></div><div class="sl-row"><label class="sl-muted" for="sl-speed">速度</label><input id="sl-speed" aria-label="播放速度" type="range" min="0.5" max="2" step="0.25" value="1"><button data-action="decode-next">续写一步</button></div></div>`);
panels.bottom.el.querySelector('.sl-progress').insertAdjacentHTML('afterend','<div class="sl-scrubbar"><label for="sl-time-scrub">动作回放</label><input id="sl-time-scrub" type="range" min="0" max="5.2" step="0.01" value="0" aria-label="当前动作的回放进度"><span id="sl-view-time">0.00 s</span></div>');
panel('diagram','sl-diagram',`<div class="sl-row sl-between"><h2 id="sl-diagram-title">模型网络</h2><button data-action="close-diagram">关闭 ×</button></div><div class="sl-diagram-body" id="sl-diagram-body"></div>`);panels.diagram.obj.visible=false;panels.diagram.el.style.display='none';
const $=id=>document.getElementById(id);
function assignment(){return L.assignment(state.model,state.layer,state.weightBytes,state.expert);}
function locate(){const a=assignment(),root=['afd','tray','router','combine','return'].includes(steps[index].id);state.rack=root?a.rootRack??a.rack:a.rack;state.slot=root?a.rootSlot??a.slot:a.slot;state.chip=root?a.root:state.model==='flash'?a.peer:a.root;}
function regenerate(preserve){const old=preserve?steps[index].id:null;steps=workflow();index=Math.max(0,steps.findIndex(s=>s.id===old));elapsed=0;complete=false;locate();render();showLevel(follow?steps[index].level:level,true);}
function flow(){let num=0;const active=steps[index].group;return N.groups.map(([id,name])=>{const rows=steps.filter(s=>s.group===id);if(!rows.length)return'';return`<details ${active===id?'open':''} data-group="${id}"><summary>${name} <span>${rows.length}</span></summary>${rows.map(s=>{const i=steps.indexOf(s);num++;return`<button class="sl-step" data-step="${i}" aria-pressed="${i===index}"><i>${String(num).padStart(2,'0')}</i><span>${esc(s.title)}<small>${esc(s.hardware)} · ${esc(s.unit)}</small></span></button>`;}).join('')}</details>`;}).join('');}
function render(){const st=steps[index],c=currentShape(),cost=N.cost(state,st),a=assignment();
 $('sl-model').value=state.model;$('sl-layer').innerHTML=Array.from({length:c.m.layers},(_,i)=>`<option value="${i+1}">L${String(i+1).padStart(2,'0')} · ${(i+1)%4===0?(c.flash?'QSA':'Gated Attention'):'DeltaNet'}${c.flash&&i===1?' + PLE':''}</option>`).join('');$('sl-layer').value=state.layer;
 document.querySelectorAll('#spatial-lab [data-mode]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.mode===state.routeMode)));
 $('sl-model-hint').textContent=N.shape(state).flash?'48 层 · 四路残差 · Top-10 + 共享专家':'64 层 · 3 DeltaNet + 1 Attention 循环';
 $('sl-flow').innerHTML=flow();$('sl-instruction').textContent=st.unit+' · '+st.title;$('sl-data').textContent='「'+c.token.text+'」 → '+st.output;
 $('sl-time-label').textContent=cost.ns===null?'当前算子耗时':'理想资源下界';$('sl-time').innerHTML=cost.ns===null?'未公开':(cost.ns>=1000?(cost.ns/1000).toFixed(2)+'<small>μs</small>':cost.ns.toFixed(2)+'<small>ns</small>');
 $('sl-byte-label').textContent=st.weightBytes?'本次权重读取':'数据 / 状态规模';$('sl-bytes').textContent=bytes(cost.bytes);$('sl-flops').textContent=cost.flops?fnum(cost.flops)+' FLOPs':'—';$('sl-hw').textContent=st.hardware;$('sl-context').textContent=`T=${c.T} / S=${c.S}`;
 $('sl-step-title').textContent=st.title;$('sl-formula').textContent=st.formula;$('sl-detail').textContent=st.detail;$('sl-rule').textContent=cost.rule+(cost.bandwidth?' 带宽：'+(cost.bandwidth>=1e12?(cost.bandwidth/1e12)+' TB/s':(cost.bandwidth/1e9)+' GB/s')+'。':'');
 $('sl-part').textContent=manualPart||levelNames[level]+' · '+(level==='chip'?'Groq 3 功能分区':'点击实体可检查，双击可深入');
 $('sl-placement').textContent=c.prefill?'Prefill 在 GPU 完成全部层；LPX 机架用于 Decode 的 FFN / MoE。':`示例部署 · 架 ${state.rack} / 槽 ${String(state.slot).padStart(2,'0')} / LPU ${state.chip}。`+(c.flash?`当前 E${state.expert}；编号来自给定路由示例，非本句实测。`:'同一 Token 激活送到 TP2 两片，各算一半 FFN 通道。');
 if(level==='gpu'){$('sl-part').textContent=manualPart||'NVIDIA Rubin · 2 compute dies / 8 HBM4';$('sl-placement').textContent='224 SM · 288 GB HBM4 · 22 TB/s。'+(st.unit.includes('矩阵')?'矩阵乘在 SM 的 Tensor Core 执行。':st.unit.includes('向量')?'向量 / 归约由 SM 执行资源处理，Softmax 的 exp 使用 SFU 路径。':'权重、KV 与状态经 HBM / 片上存储层级访问。')+'落点表示资源类别，具体 HBM 栈与 SM 分配未公开。';}
 $('sl-expert-row').style.display=c.flash?'flex':'none';$('sl-expert').value=state.expert;$('sl-precision').value=state.weightBytes;
 $('sl-source-links').replaceChildren();const refs=[['模型配置',N.configSources[state.model]],['网络实现',N.implementationSources[state.model]],['官方 Tokenizer',c.s.tokenizerUrl],['官方对话模板',c.s.chatTemplateUrl],['NVIDIA Rubin 官方结构',RUBIN_SOURCES.article],['Rubin 官方封装图',RUBIN_SOURCES.figure],['NVIDIA Groq 3 LPX 架构',L.sources?.article||'https://developer.nvidia.com/blog/inside-nvidia-groq-3-lpx-the-low-latency-inference-accelerator-for-the-nvidia-vera-rubin-platform/'],['渲染参考 · Branch Education','https://www.youtube.com/watch?v=fCS8jGc3log']];for(const[label,url]of refs){const link=document.createElement('a');link.textContent=label+' ↗';link.href=typeof url==='string'?url:'https://developer.nvidia.com/';link.target='_blank';link.rel='noreferrer';$('sl-source-links').append(link);}
 $('sl-tokens').innerHTML=(c.prefill?c.s.tokens:c.s.answerTokens).map((t,i)=>`<button data-token="${i}" aria-pressed="${i===(c.prefill?state.tokenIndex:state.outputIndex)}">${esc(t.text)}<small>${t.id}</small></button>`).join('');
 panels.bottom.el.querySelector('.sl-prompt span').textContent=c.prefill?c.s.prompt:'给定续写回放 · '+c.s.answer;
 $('sl-timeline').innerHTML=steps.map((step,i)=>'<button data-step="'+i+'" title="'+esc(step.title)+'" class="'+(step.hardware==='LPX'?'lpu':'gpu')+'" aria-pressed="'+(i===index)+'"><span>'+(i%5===0?i+1:'')+'</span></button>').join('');
 $('sl-request-status').textContent=state.routeMode==='request'?'请求 A · Prompt 只输入一次 → GPU Prefill → GPU / LPX Decode':state.routeMode==='prefill'?'仅查看 Prefill · 全部在 GPU':'仅查看 Decode · 复用已有前缀缓存';
 $('sl-counter').textContent=`${index+1} / ${steps.length}`;panels.bottom.el.querySelector('[data-action=prev]').disabled=index===0;panels.bottom.el.querySelector('[data-action=next]').disabled=index===steps.length-1;
 panels.bottom.el.querySelector('[data-action=play]').disabled=complete;panels.bottom.el.querySelector('[data-action=play]').textContent=complete?'已完成':running?'暂停':'播放';panels.bottom.el.querySelector('[data-action=decode-next]').textContent='下一 Token';panels.bottom.el.querySelector('[data-action=decode-next]').disabled=!(complete&&st.id==='sample')||(state.mode!=='prefill'&&state.outputIndex>=c.s.answerTokens.length-2);updateNav();
}
function updateNav(){panels.toolbar.el.querySelectorAll('[data-level]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.level===level)));panels.toolbar.el.querySelector('[data-action=follow]').setAttribute('aria-pressed',String(follow));panels.toolbar.el.querySelector('[data-action=explode]').setAttribute('aria-pressed',String(explosionTarget>.25));
 panels.caption.el.textContent=(follow?'执行视角':'自由观察')+' · '+levelNames[level]+(steps[index].level!==level?' / 当前算子位于 '+levelNames[steps[index].level]:'')+' · 拖动旋转 / 滚轮缩放 / 双击深入';}
function disposeObject(object){object.traverse(n=>{if(n.geometry)n.geometry.dispose();if(n.material){const ms=Array.isArray(n.material)?n.material:[n.material];for(const m of ms){if(m.map)m.map.dispose();m.dispose();}}});}
const cache=new Map();
function showLevel(next,rebuild=false){const changed=level!==next;level=next;manualPart='';if(follow)locate();const key=[level,state.rack,state.slot,state.chip].join('/');if(hardware)scene.remove(hardware.group);if(!cache.has(key)){const hw=buildHardware(level,state);cache.set(key,hw);}hardware=cache.get(key);scene.add(hardware.group);picks=hardware.picks();
 explosion=0;explosionTarget=level==='tray'?1:level==='rack'?.15:0;hardware.setExplosion(explosion);floor.position.y=['rack','external'].includes(level)?-6.25:-.6;
 grid.position.y=floor.position.y+.01;if(changed||rebuild)fit();buildData();render();resize();}
function fit(){const v={external:[14,7,25],rack:[13,6,23],tray:[13,16,19],fabric:[11,18,16],chip:[9,15,16],gpu:[10,14,17]}[level];const available=Math.max(.3,(innerWidth-(layoutInfo.leftWidth||270)-(layoutInfo.rightWidth||270)-80)/innerWidth);const ratio=Math.max(1,(.52/available)**.65);camera.position.set(...v).multiplyScalar(ratio);controls.target.set(0,level==='external'?-1:level==='rack'?-.2:0,0);controls.update();}
function sprite(words,color='#157b83',size=.32){const cn=document.createElement('canvas'),cx=cn.getContext('2d');cx.font='600 70px "Microsoft YaHei", sans-serif';cn.width=Math.ceil(cx.measureText(words).width+40);cn.height=110;cx.font='600 70px "Microsoft YaHei", sans-serif';cx.textAlign='center';cx.textBaseline='middle';cx.shadowColor='rgba(247,255,249,.98)';cx.shadowBlur=10;cx.lineWidth=6;cx.strokeStyle='rgba(247,255,249,.85)';cx.strokeText(words,cn.width/2,55);cx.fillStyle=color;cx.fillText(words,cn.width/2,55);const tex=new T.CanvasTexture(cn);tex.colorSpace=T.SRGBColorSpace;const sp=new T.Sprite(new T.SpriteMaterial({map:tex,transparent:true,depthTest:false,depthWrite:false}));sp.scale.set(size*1.3*cn.width/110,size*1.3,1);sp.renderOrder=10;return sp;}
function anchor(id){let a=hardware.anchors[id];if(id==='ROOT'||id==='PEER'){const as=assignment(),isRoot=id==='ROOT';a=hardware.anchors['CHIP'+(isRoot?as.root:as.peer)];if(as.rootRack!==as.rack||as.rootSlot!==as.slot){const inside=isRoot?state.rack===as.rootRack&&state.slot===as.rootSlot:state.rack===as.rack&&state.slot===as.slot;if(!inside)a=hardware.anchors.SPINE;}}return(a||hardware.anchors.MEM||hardware.anchors.GPU||new T.Vector3()).clone();}
function makeCurve(a,b,offset=0){const mid=a.clone().lerp(b,.5);mid.y+=Math.max(1,a.distanceTo(b)*.22)+offset;mid.z+=offset*.3;return new T.CatmullRomCurve3([a,mid,b]);}
function route(curve,color,count=18,tag=null){const line=new T.Line(new T.BufferGeometry().setFromPoints(curve.getPoints(70)),new T.LineBasicMaterial({color,transparent:true,opacity:.35,depthWrite:false}));dataLayer.add(line);const bead=new T.SphereGeometry(.036,8,6),mat=new T.MeshBasicMaterial({color});const dots=new T.InstancedMesh(bead,mat,count);dots.frustumCulled=false;dataLayer.add(dots);let label=null;if(tag){label=sprite(tag,'#20768b',.38);dataLayer.add(label);}trajectories.push({curve,dots,count,label,line});}
function buildData(){disposeObject(dataLayer);dataLayer.clear();trajectories=[];signal=null;activeLabel=null;if(steps[index].level!==level)return;const st=steps[index],c=currentShape();let a=anchor(st.from),b=anchor(st.to);if(a.distanceTo(b)<.1){if(level==='gpu'){a.add(new T.Vector3(-.55,.1,0));b.add(new T.Vector3(.55,.1,0));}else b.add(new T.Vector3(1.5,0,1));}
 if(st.kind==='tokens'||st.kind==='text'||st.kind==='output'){const ts=st.kind==='output'?[c.s.answerTokens[c.prefill?0:Math.min(state.outputIndex+1,c.s.answerTokens.length-1)]]:st.kind==='text'?[{text:c.s.prompt}]:c.s.tokens;for(let i=0;i<ts.length;i++){const offset=(i-(ts.length-1)/2)*.8,aa=a.clone().add(new T.Vector3(offset,.25,0)),bb=b.clone().add(new T.Vector3(offset,.25,0)),cur=makeCurve(aa,bb,i*.05);const label=sprite(ts[i].text,['#27868c','#4e758f','#8c713e','#50825a','#786393','#24747d'][i%6],.48);dataLayer.add(label);trajectories.push({curve:cur,label,phase:i/ts.length,noDots:true});}route(makeCurve(a,b),0x3a9d9e,15);}
 else if(level==='tray'&&st.kind==='transport'){const fab=anchor('FABRIC').add(new T.Vector3(0,.4,0));a.y+=.3;b.y+=.3;const p=a.clone().lerp(fab,.5).add(new T.Vector3(0,.45,0)),q=fab.clone().lerp(b,.5).add(new T.Vector3(0,.45,0));route(new T.CatmullRomCurve3([a,p,fab,q,b]),0x31a6b5,32,c.token.text);}
 else if(st.kind==='dispatch'||st.kind==='combine'){const as=assignment(),owners=c.flash?L.exampleExperts.map(e=>L.assignment(state.model,state.layer,state.weightBytes,e)):[as],seen=new Set();let j=0;for(const owner of owners){const local=owner.rack===state.rack&&owner.slot===state.slot,key=local?'CHIP'+owner.peer:'SPINE';if(seen.has(key))continue;seen.add(key);const target=anchor(key),start=st.kind==='combine'?target:a,end=st.kind==='combine'?b:target;if(start.distanceTo(end)<.05)continue;const selected=owner.globalPeer===as.globalPeer;route(makeCurve(start,end,.2+j*.09),selected?0x31a6b5:0x90ac8c,12,selected?c.token.text:null);j++;}}
 else{const n=['qkv','expand','router','expert','logits'].includes(st.kind)?st.kind==='expand'?4:3:6;for(let i=0;i<n;i++){const off=(i-(n-1)/2)*.09;const aa=a.clone().add(new T.Vector3(off,.10,off)),bb=b.clone().add(new T.Vector3(off,.10,off));route(makeCurve(aa,bb,Math.abs(off)),[0x3596ad,0x8fac69,0xc09955,0x937bb6][i%4],18,i===Math.floor(n/2)?c.token.text:null);}
 if(['qkv','expand','router','softmax','gate','state','logits'].includes(st.kind)){const tags=st.kind==='qkv'?['Q','K','V']:st.kind==='expand'?['H₀','H₁','H₂','H₃']:st.kind==='router'?['10 / 512','+ shared']:st.kind==='state'?['S [48,128,128]']:st.kind==='softmax'?['max → exp → Σ']:st.kind==='logits'?['248320 logits']:['SiLU ⊙'];tags.forEach((tag,i)=>{const label=sprite(tag,['#478d9c','#858f48','#aa8257','#8a6da2'][i%4],.27);label.position.copy(b).add(new T.Vector3((i-(tags.length-1)/2)*1.2,1.0,0));dataLayer.add(label);});}}
 const at=level==='chip'?anchor('ICU'):a.clone().add(new T.Vector3(-.45,.15,.55));const sigcurve=makeCurve(at,b,.75);route(sigcurve,0xa588ce,7);signal=sprite(st.unit+' · '+(st.unit==='MXM'?'矩阵乘':st.unit==='VXM'?'向量运算':st.unit==='C2C'?'SEND / RECV':'调度'), '#8568a4',.25);signal.position.copy(sigcurve.getPoint(.48)).add(new T.Vector3(0,.6,0));dataLayer.add(signal);
 if(!['text','tokens','output'].includes(st.kind)){activeLabel=sprite(c.token.text+'  ·  '+(st.kind==='embedding'?c.token.id:st.output.replace(/\[|\]/g,'')), '#286d72',.31);activeLabel.position.copy(a).add(new T.Vector3(0,1.25,0));dataLayer.add(activeLabel);}
}
const dummy=new T.Object3D();function animateData(){const progress=Math.min(1,elapsed/duration);for(const tr of trajectories){if(level==='rack'&&steps[index].level==='rack'){tr.curve.points.at(-1).z=2.32+3.8*explosion;if(tr.line)tr.line.geometry.setFromPoints(tr.curve.getPoints(70));}if(tr.dots){for(let i=0;i<tr.count;i++){const flight=(progress-i/tr.count*.25)/.62,p=T.MathUtils.clamp(flight,0,1);dummy.position.copy(tr.curve.getPoint(p));dummy.scale.setScalar(flight<0||flight>1?.0001:1);dummy.updateMatrix();tr.dots.setMatrixAt(i,dummy.matrix);}tr.dots.instanceMatrix.needsUpdate=true;}if(tr.label){const t=T.MathUtils.clamp((progress-(tr.phase||0)*.25)/.62,0,1);tr.label.position.copy(tr.curve.getPoint(t));tr.label.position.y+=.2;tr.label.userData.arrived=t===1;}}}
function seek(next){index=Math.max(0,Math.min(steps.length-1,next));elapsed=0;complete=false;follow=true;locate();showLevel(steps[index].level);}
function toggleRun(){if(complete)return;running=!running;render();}
function openDiagram(template=false){running=false;diagramOpen=true;panels.diagram.obj.visible=true;panels.diagram.el.style.display='flex';const c=currentShape();$('sl-diagram-title').textContent=template?'官方分词 · 正文与对话模板':c.m.name+' · 完整网络';
 if(template){$('sl-diagram-body').innerHTML=`<p class="sl-spec">正文 6 个 Token。单条 user 消息、关闭 thinking、加 assistant 生成前缀后为 18 个 Token。</p><div style="display:flex;flex-wrap:wrap;gap:18px;padding:24px 0">${c.s.contextTokens.map(t=>`<div style="color:#${c.s.tokens.some(v=>v.id===t.id)?'297d7f':'856a9e'}"><span style="font:14px Consolas,monospace">${esc(t.text.replace(/\n/g,'↵'))}</span><small style="display:block;color:#829187">${t.id}</small></div>`).join('')}</div><p class="sl-spec">正文分词可点击底部字片逐一追踪。特殊 Token 与换行参与模型输入，但不属于用户句子的正文。</p>`;}
 else{const svg=window.HARDWARE_SVGS[state.model==='flash'?'networkflash':'network27']?.svg;$('sl-diagram-body').innerHTML=(svg||'<p>网络图未构建。</p>')+'<p class="sl-muted">点击左侧层选择器展开任意层：每 4 层切换一次 Attention 类型。Flash-Next 第 2 层另有 PLE；MTP 推测解码不在本轮语言回放中。</p>';}
 resize();render();}
function nextDecode(){if(!complete||steps[index].id!=='sample')return;const fromPrefill=state.mode==='prefill';if(!fromPrefill&&state.outputIndex>=N.shape(state).s.answerTokens.length-2)return;state.outputIndex=fromPrefill?0:state.outputIndex+1;state.mode='decode';state.routeMode='decode';steps=workflow();index=steps.findIndex(s=>s.id==='embedding');elapsed=0;complete=false;follow=true;showLevel('gpu',true);}
function seekTime(seconds){running=false;elapsed=T.MathUtils.clamp(Number(seconds)||0,0,duration);complete=index===steps.length-1&&elapsed===duration;animateData();render();}
host.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;if(b.dataset.step!==undefined){seek(Number(b.dataset.step));return;}if(b.dataset.mode){state.routeMode=b.dataset.mode;state.mode=b.dataset.mode==='prefill'?'prefill':'decode';follow=true;regenerate(false);return;}if(b.dataset.level){follow=false;showLevel(b.dataset.level);return;}if(b.dataset.token!==undefined){if(currentShape().prefill)state.tokenIndex=+b.dataset.token;else state.outputIndex=+b.dataset.token;regenerate(true);return;}switch(b.dataset.action){case'play':toggleRun();break;case'reset':running=false;seek(0);break;case'top':camera.position.copy(controls.target).add(new T.Vector3(0,23,.01));controls.update();break;case'front':camera.position.copy(controls.target).add(new T.Vector3(0,.01,26));controls.update();break;case'next':seek(index+1);break;case'prev':seek(index-1);break;case'follow':follow=!follow;if(follow)showLevel(steps[index].level);else render();break;case'fit':fit();break;case'explode':explosionTarget=explosionTarget>.25?0:1;updateNav();break;case'network':openDiagram();break;case'template':openDiagram(true);break;case'close-diagram':diagramOpen=false;panels.diagram.obj.visible=false;panels.diagram.el.style.display='none';break;case'decode-next':nextDecode();break;case'sources':$('sl-sources').open=true;$('sl-sources').scrollIntoView({block:'nearest'});break;}});
 $('sl-model').onchange=e=>{state.model=e.target.value;state.layer=1;state.tokenIndex=0;state.outputIndex=0;regenerate(false);};$('sl-layer').onchange=e=>{state.layer=+e.target.value;steps=workflow();index=steps.findIndex(s=>s.group==='attention');elapsed=0;follow=true;showLevel(steps[index].level,true);};$('sl-expert').onchange=e=>{state.expert=+e.target.value;regenerate(true);};$('sl-precision').onchange=e=>{state.weightBytes=+e.target.value;regenerate(true);};$('sl-speed').oninput=e=>{speed=+e.target.value;};
const ray=new T.Raycaster(),pointer=new T.Vector2();let downPos=null;
function hit(event){const r=renderer.domElement.getBoundingClientRect();pointer.set((event.clientX-r.left)/r.width*2-1,-(event.clientY-r.top)/r.height*2+1);ray.setFromCamera(pointer,camera);return ray.intersectObjects(picks,false)[0]?.object.userData.pick;}
renderer.domElement.addEventListener('pointerdown',e=>downPos=[e.clientX,e.clientY]);renderer.domElement.addEventListener('pointerup',e=>{if(!downPos||Math.hypot(e.clientX-downPos[0],e.clientY-downPos[1])>5)return;const h=hit(e);if(h)selectPart(h,false);});renderer.domElement.addEventListener('dblclick',e=>{const h=hit(e);if(h)selectPart(h,true);});
function selectPart(h,enter=false){if(h.part==='SLOT'){state.slot=h.slot;follow=false;showLevel(enter?'tray':'rack',enter);}if(h.part==='LPU'){state.chip=h.chip;follow=false;if(enter)showLevel('chip',true);}if(h.part==='GPU'&&enter){follow=false;showLevel('gpu',true);}const info=h.part==='LPU'?L.chipInfo(state):null;manualPart=h.part==='HBM4'?'HBM4 栈 '+h.stack+' · 权重 / KV 存储资源':h.part==='RUBIN_DIE'?'Rubin 计算裸片 '+h.die+' · SM / Tensor Core / SFU':h.part==='SLOT'?'槽位 '+String(h.slot).padStart(2,'0'):h.part==='LPU'?'LPU '+h.chip+' · '+info.label:h.region||h.part;render();}
renderer.domElement.addEventListener('keydown',e=>{if(e.code==='Space'){e.preventDefault();toggleRun();}else if(e.code==='ArrowRight')seek(index+1);else if(e.code==='ArrowLeft')seek(index-1);});
function resize(){const w=host.clientWidth,h=host.clientHeight;if(!w||!h)return;renderer.setSize(w,h);css.setSize(w,h);camera.aspect=w/h;camera.updateProjectionMatrix();const margin=0,gap=1,leftWidth=w<950?180:w<1300?220:260,rightWidth=w<950?210:w<1300?250:290;const centerStart=margin+leftWidth+gap,centerWidth=w-centerStart-rightWidth-margin-gap;layoutInfo={leftWidth,rightWidth,centerWidth};const top=32,bottom=0,hudH=184;
 const rects={title:[0,0,w,31],left:[0,top,leftWidth,h-top-hudH-1],right:[w-rightWidth,top,rightWidth,h-top],toolbar:[centerStart,top,centerWidth,31],caption:[centerStart,top+32,centerWidth,26],bottom:[0,h-hudH,w-rightWidth-1,hudH],diagram:[centerStart,top+60,centerWidth,h-top-hudH-62]};
 if(centerWidth<300){rects.left[2]=Math.max(150,leftWidth-24);rects.right[0]+=24;rects.right[2]-=24;rects.toolbar[0]-=24;rects.toolbar[2]+=48;rects.bottom[0]-=24;rects.bottom[2]+=48;}
 for(const[id,r]of Object.entries(rects)){const p=panels[id];p.rect=r;p.el.style.width=r[2]+'px';p.el.style.height=r[3]+'px';}positionPanels();css.render(scene,camera);}
function positionPanels(){const w=host.clientWidth,h=host.clientHeight;if(!h)return;const distance=7,unit=2*distance*Math.tan(T.MathUtils.degToRad(camera.fov)/2)/h;for(const p of Object.values(panels)){if(!p.rect)continue;const[x,y,pw,ph]=p.rect;p.obj.position.set((x+pw/2-w/2)*unit,-(y+ph/2-h/2)*unit,-distance).applyQuaternion(camera.quaternion).add(camera.position);p.obj.quaternion.copy(camera.quaternion);p.obj.scale.setScalar(unit);}}
document.getElementById("sl-time-scrub").oninput=e=>seekTime(e.target.value);
window.addEventListener('resize',resize);window.addEventListener('groq:pause',()=>{running=false;});document.addEventListener('visibilitychange',()=>{last=0;});
function activate(){window.GROQ_LAB?.stop();window.dispatchEvent(new Event('groq:pause'));document.body.dataset.workspace='spatial';resize();}
function frame(ms){requestAnimationFrame(frame);if(document.body.dataset.workspace!=='spatial'||document.hidden){last=ms;return;}const dt=last?Math.min((ms-last)/1000,.08):0;last=ms;if(running&&!diagramOpen){elapsed+=dt*speed;if(elapsed>=duration){if(index<steps.length-1){seek(index+1);}else{elapsed=duration;running=false;complete=true;render();}}}controls.update();explosion+=(explosionTarget-explosion)*Math.min(1,dt*5);hardware.setExplosion(explosion);animateData();positionPanels();$('sl-progress').style.width=Math.min(100,elapsed/duration*100)+'%';document.getElementById("sl-time-scrub").value=elapsed;document.getElementById("sl-view-time").textContent=elapsed.toFixed(2)+" s";renderer.render(scene,camera);css.render(scene,camera);}
window.SPATIAL_LAB_APP={scene,camera,controls,renderer,css,panels,dataLayer,activate,seek,setLevel(l){follow=false;showLevel(l,true);},setModel(id){state.model=id;state.layer=1;regenerate(false);},setMode(mode){state.routeMode=mode;state.mode=mode==='prefill'?'prefill':'decode';regenerate(false);},setLayer(n){state.layer=n;regenerate(true);},play:toggleRun,fit,selectPart,seekTime,get hardware(){return hardware;},get state(){return{...state,index,level,follow,running,elapsed,complete,step:steps[index].id,steps:steps.map(s=>s.id),explosion};}};
activate();showLevel('external',true);requestAnimationFrame(frame);
}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
